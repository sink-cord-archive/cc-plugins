import {
    BUNDLED_LANGUAGES,
    BUNDLED_THEMES,
    FontStyle,
    getHighlighter,
    loadTheme,
    renderToHtml,
    setCDN,
    setOnigasmWASM,
    setWasm,
    toShikiTheme,
} from "shiki";

export default {
    BUNDLED_LANGUAGES,
    BUNDLED_THEMES,
    FontStyle,
    getHighlighter,
    loadTheme,
    renderToHtml,
    setCDN,
    setOnigasmWASM,
    setWasm,
    toShikiTheme,
};
